# 中国境内可访问部署说明

这个项目是纯静态网页，可以部署到对象存储静态网站服务。为了在中国境内访问更稳定，优先选择国内云厂商：

- 推荐一：阿里云 OSS 静态网站托管
- 推荐二：腾讯云 COS 静态网站托管

## 推荐方案

如果只是自己和家人使用，建议先用国内云对象存储的默认访问地址测试。  
如果要绑定自己的域名，并希望在中国内地稳定访问，通常需要已备案域名。

## 需要上传的文件

上传本目录中的所有文件：

- `index.html`
- `styles.css`
- `app.js`
- `manifest.webmanifest`
- `sw.js`
- `icon.svg`
- `.nojekyll`

## 阿里云 OSS 大致步骤

1. 创建 Bucket，地域选中国内地离你近的区域，例如华东、华南。
2. 上传本目录全部文件到 Bucket 根目录。
3. 开启静态网站托管。
4. 默认首页设置为 `index.html`。
5. 如果有错误页面设置，也填 `index.html`。
6. 设为公共读，或按控制台提示设置公开访问策略。
7. 复制静态网站访问地址，用手机流量测试。

## 腾讯云 COS 大致步骤

1. 创建存储桶，地域选中国内地离你近的区域。
2. 上传本目录全部文件到存储桶根目录。
3. 开启静态网站功能。
4. 索引文档设置为 `index.html`。
5. 错误文档可以设置为 `index.html`。
6. 设为公有读，或按控制台提示设置公开访问策略。
7. 复制静态网站访问地址，用手机流量测试。

## 手机添加到桌面

部署后，用手机浏览器打开公网地址：

- iPhone：Safari 里点分享按钮，选择“添加到主屏幕”。
- Android：浏览器菜单里选择“添加到主屏幕”或“安装应用”。

## 数据说明

排班数据保存在当前设备浏览器的 `localStorage`。  
手机上添加的排班会保存在手机浏览器里，电脑和手机之间不会自动同步。
